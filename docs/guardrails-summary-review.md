# Guardrails G-01 – G-09: Summary Table — Implementation Review

> **Review date:** 2026-06-22  
> **Reviewer:** Copilot (automated code vs. doc comparison)  
> **Source files checked:**  
> - `guardrails-service/app/core/guards/*.py`  
> - `guardrails-service/app/core/guards/azure_endpoints.py`  
> - `bankbuddy/policies/bankbuddy-default.yaml`

The **Updated** column flags every value in the original document that does not match the live source code. Policy-level overrides are distinguished from code defaults where both differ.

---

| Guard ID | Guard Name | Purpose | Azure? | Key Config / Implementation | Updated |
|---|---|---|---|---|---|
| **G-01** | `azure-content-safety` (harm analysis) | Hate / SelfHarm / Sexual / Violence harm classification on text. | **Yes** | Azure AI Content Safety REST `text:analyze` (`api-version=2024-09-01`); categories `["Hate","SelfHarm","Sexual","Violence"]`; `severity_threshold` 0/2/4/6 (**code default 4**; policy overrides to 2); `timeout_seconds` (**code default 5s**; policy sets 3s); `fail_open` (**code default true**; **api_input policy explicitly sets `fail_open: false`** at the trust boundary). Async `httpx`; runs in parallel with Prompt Shields. | ⚠️ **Three values differ from document:** (1) `severity_threshold` code default is **4**, not 2 — 2 is the policy override. (2) `timeout_seconds` code default is **5s**, not 3s — 3s is the policy override. (3) `fail_open` is **false in the api_input policy** (trust boundary), not true as stated. |
| **G-02a** | `token-limit` | DoS guard — oversize text rejection before LLM tokens are spent. | No | Pure character count: `len(text) > max_chars`; **code default `max_chars=8000`**; policy sets `max_chars=50000` at api_input/tool_input. No deps. | ⚠️ Document states "default max_chars=50000". **Code default is 8,000.** 50,000 is the policy-level override, not the guard's built-in default. |
| **G-02b** | `prompt-injection` (heuristic) | Jailbreak / role-override / script & XXE injection detection. | No | Regex catalog (**19 patterns**, not ~18) with per-pattern weights summed against `block_threshold` (default 0.7). Patterns include `\bignore (all\|any\|previous\|prior\|the above) (instructions?\|prompts?\|rules?)\b` (0.9), `\b(you are\|act as\|pretend to be) (?:a \|an )?(?:DAN\|jailbroken\|unrestricted)\b` (0.95), `</?(system\|assistant\|user)>` (0.8), `<script[\s>]`, `javascript\s*:`, `on(?:error\|load\|click\|mouseover)\s*=`, `<!DOCTYPE\s+\w[\w\s]*\[`, `<!ENTITY\s+\w+\s+SYSTEM\b`. Supports `patterns` (override) and `extra_patterns` (merge). Lib: `re`. | ⚠️ Document says "~18 patterns". **Code has 19 patterns.** Minor; count should read ~19. |
| **G-02c** | `azure-content-safety` (Prompt Shields) | Managed direct + indirect prompt injection / jailbreak detection. | **Yes** | Azure AI Content Safety `text:shieldPrompt` (`api-version=2024-09-01`); enabled via `enable_prompt_shield: true` on input & tool_output stages; runs concurrently with harm analysis. | No change. |
| **G-02d** | `sql-injection` | SQL injection patterns in user input, tool args, and tool outputs. | No | Regex catalog (~25 patterns), case-insensitive. Includes comment markers `(^\|\s)--\s`, `/\*.*?\*/`; stacked statements `;\s*(select\|insert\|update\|delete\|drop\|alter\|create\|truncate\|grant\|revoke)\b`; union `\bunion\s+(all\s+)?select\b`; tautologies `\bor\s+1\s*=\s*1\b`, `\bor\s+'[^']+'\s*=\s*'[^']+'`, `\band\s+1\s*=\s*1\b`; time-based blind `\bsleep\s*\(`, `\bpg_sleep\s*\(`, `\bwaitfor\s+delay\b`, `\bbenchmark\s*\(`; OOB exfil `\bload_file\s*\(`, `\binto\s+outfile\b`, `\bxp_cmdshell\b`, `\butl_http\b`. Supports `ignore_fields` to skip free-text JSON keys. BLOCK on any match. Lib: `re`, `json`. | No change. |
| **G-03a** | `azure-topic-relevance` | Managed scope / intent classification via trained Custom Categories. | **Yes** | Azure AI Content Safety `text:analyzeCustomCategory` (`api-version=2024-09-15-preview`); requires pre-trained custom categories; one API call per category fanned out concurrently; `severity_threshold` default 2; `fail_open=true`. | No change. |
| **G-03b** | `topic-relevance` (local) | Heuristic scope filter — fallback when Azure custom category not configured. | No | Tokenizes input via `[A-Za-z][A-Za-z0-9'-]+`; ratio = matched_keywords / meaningful_words; BLOCK if `ratio < min_ratio` (default 0.05); skips inputs shorter than `min_length` (**code default 3 chars**; policy sets 20). Keywords derived either from explicit `keywords:` list or from `task_definition` free text. Lib: `re`. | ⚠️ Document states "skips inputs shorter than `min_length` (20 chars)". **Code default is 3**, not 20. 20 is the policy-level override (`min_length: 20` in bankbuddy-default.yaml). |
| **G-03a (Task Adherence)** | `azure-task-adherence` — **Not in use (unavailable in central-us deployment)** | Validates a planned tool call aligns with declared task scope. | **Yes** | Azure AI Content Safety `agent:analyzeTaskAdherence` (**`api-version=2025-03-01`**, not preview); policy-driven `task_definitions: {tool_name: description}`; runs at `tool_input`; BLOCK when `taskRiskDetected=true`; `fail_open=true`. *Disabled in policy; G-15 `schema-enforcement` allowlist covers the tool-scope concern in the interim.* | ⚠️ Document states `api-version=2025-09-15-preview`. **Code uses `CONTENT_SAFETY_API_VERSION_TASK_ADHERENCE = "2025-03-01"`** (set in `azure_endpoints.py`). The preview suffix in the document is incorrect. |
| **G-04a** | `azure-pii-detection` — **Not in use (unavailable in central-us deployment)** | Managed PII detection (SSN, credit card, IBAN, phone, email, address, etc.). | **Yes** | Azure AI Language `language/:analyze-text` (`api-version=2023-04-01`), `kind=PiiEntityRecognition`. Requires dedicated `AZURE_LANGUAGE_ENDPOINT` + `AZURE_LANGUAGE_KEY`. `min_confidence` default 0.5; `mode: block \| sanitize`; category allowlist applied in policy; `fail_open=true`. *Disabled in policy; G-04b `pii-detect` (Presidio + regex fallback) runs as the production PII control.* | No change. |
| **G-04b** | `pii-detect` | Local PII detection via Microsoft Presidio with regex fallback. | No | Primary: Microsoft Presidio (`presidio-analyzer`) + spaCy NLP (`en_core_web_sm`). **Code default `min_score=0.4`**; policy sets `min_score=0.35`; `mode=sanitize`; entities include `EMAIL_ADDRESS`, `PHONE_NUMBER`, `CREDIT_CARD`, `US_SSN`, `US_BANK_NUMBER`, `US_DRIVER_LICENSE`, `US_ITIN`, `US_PASSPORT`, `IBAN_CODE`, `IP_ADDRESS`, `CRYPTO`, `MEDICAL_LICENSE`, `LOCATION`, `PERSON`. Regex fallback when Presidio missing: email, SSN, credit card, US phone, international phone (`\+\d{1,3}[\s\-.][ \d\s\-.]{6,}\d`), IPv4, IBAN, date-ISO. | ⚠️ Document states "Default `min_score=0.35`". **Code default is 0.4** (`config.get("min_score", 0.4)`). 0.35 is the policy override. Also: regex fallback includes an **international phone** pattern and a **date-ISO** pattern not mentioned in the document. |
| **G-05** | `secret-leak` | Refuse leaked API keys, tokens, private keys, and DB connection strings. | No | Regex catalog (**17 patterns**, not ~14), BLOCK on any match. Patterns include AWS access key `\bAKIA[0-9A-Z]{16}\b`, AWS secret, GitHub PAT `\bghp_[A-Za-z0-9]{36}\b`, GitLab PAT `\bglpat-[A-Za-z0-9_-]{20,}\b`, OpenAI `\bsk-[A-Za-z0-9]{20,}\b`, Stripe, JWT, Bearer, PEM, Slack token `\bxox[bpoas]-[A-Za-z0-9-]{10,}\b`, **Slack webhook** `hooks\.slack\.com/services/…`, Azure Storage `AccountKey=…`, DB URLs, **`password-value`** `(?i)\bpassword\s*(?:is\s*)?[:=]\s*\S{8,}`, **`secret-keyword`** `(?i)(?:secret\|passwd\|credential)\s*[:=]\s*\S{8,}`, **`generic-api-key`** `(?i)\bapi[_-]?key\s*[:=]\s*[A-Za-z0-9_-]{20,}`, **`token-keyword`** `(?i)\btoken\s*[:=]\s*[A-Za-z0-9._-]{20,}`. Supports `extra_patterns` for custom corp tokens. Lib: `re`. | ⚠️ Document states "~14 patterns". **Code has 17 patterns.** Five patterns are missing from the document: `slack-webhook`, `password-value`, `secret-keyword`, `generic-api-key`, `token-keyword`. |
| **G-06** | `azure-content-safety` (Protected Material) | Copyrighted text detection on LLM output. | **Yes** | Azure AI Content Safety `text:detectProtectedMaterial` (`api-version=2024-09-01`); enabled via `enable_protected_material: true` on output / api_output stages only; `fail_open=true`. | No change. |
| **G-07a** | `banned-substrings` | Local custom blocklist of phrases. | No | Exact substring match (case-insensitive by default). Sources merged & de-duped: policy `phrases:`, per-request `context.banned_phrases`/`extra_banned_phrases` when `allow_overrides=true`, built-in fallback `["ignore previous instructions","system prompt","developer mode"]`. BLOCK on any hit. No deps. | No change. |
| **G-07b** | `azure-content-safety` (Text Blocklists) — **Not in use (unavailable in central-us deployment)** | Managed corporate blocklists hosted in Azure Content Safety. | **Yes** | Same `text:analyze` call; `blocklist_names: [...]` referenced in policy; `halt_on_blocklist_hit: true` short-circuits and BLOCKs immediately on any blocklist match. *Empty `blocklist_names: []` in policy; G-07a `banned-substrings` covers the blocklist requirement.* | No change. |
| **G-08a** | `azure-groundedness` — **Not in use (unavailable in central-us deployment)** | Managed hallucination / ungrounded-claim detection against source passages. | **Yes** | Azure AI Content Safety `text:detectGroundedness` (`api-version=2024-09-01`); requires `context.sources` (and `context.query` for `task: QnA`); `domain: Generic \| Medical`; `task: QnA \| Summarization`; `require_sources` default false; `timeout_seconds=8`; `fail_open=true`. BLOCK when `ungroundedDetected=true`. *Disabled in policy; G-08b local `groundedness` runs as the production hallucination check.* | No change. |
| **G-08b** | `groundedness` (local) | Deterministic overlap-based grounding heuristic — no Azure dependency. | No | Sentence-level token overlap of the reply against `context.sources`. Engines wired: `overlap` (active); `nli`/`llm-judge`/`azure` placeholders fall back to overlap. Defaults: `block_threshold=0.45` (BLOCK below), `warn_threshold=0.65` (SANITIZE — append `unverified_suffix` between thresholds), `min_length=40`, `require_sources=false`. Lib: `re`. | No change. |
| **G-09** | `bias-detect` | Lexicon-based bias / stereotype / demographic-skew detection. | No | Regex lexicon across 4 categories with severity mapping (default: gender-role=high, demographic-skew=high, age-bias=medium, ability-bias=high). Example patterns (case-insensitive): `\bwomen\s+(?:are\s+)?(?:bad\|worse\|not\s+good)\s+at\b`, `\b(?:women\|men)\s+(?:should\|belong)\s+(?:in\|at)\s+(?:home\|kitchen)\b`, `\bpeople\s+from\s+\w+\s+are\s+(?:typically\|always\|usually)\s+(?:bad\|good\|lazy\|smart)\b`, `\b(?:old\|elderly\|young)\s+people\s+(?:cannot\|can't\|are\s+unable\s+to)\b`. BLOCK if max_severity=high, SANITIZE (replace with `[neutralized]`) if medium, log-only if low. `min_length=30`. Lib: `re`. | No change. |

---

## Summary of Changes Required

| # | Guard | Field | Document States | Actual Code / Policy | Recommendation |
|---|---|---|---|---|---|
| 1 | G-01 | `severity_threshold` | "default 2 in policy" | Code default = **4**; policy overrides to 2 | Clarify as "code default 4; policy sets 2" |
| 2 | G-01 | `timeout_seconds` | "3s" | Code default = **5s**; policy sets 3s | Clarify as "code default 5s; policy sets 3s" |
| 3 | G-01 | `fail_open` | "fail_open=true" | Code default = true; **api_input policy = false** | Note that api_input overrides to `fail_open: false` at the trust boundary |
| 4 | G-02a | `max_chars` | "default max_chars=50000" | Code default = **8,000**; policy sets 50,000 | Correct to "code default 8,000; policy sets 50,000" |
| 5 | G-03a (Task Adherence) | `api_version` | `2025-09-15-preview` | `CONTENT_SAFETY_API_VERSION_TASK_ADHERENCE = "2025-03-01"` in `azure_endpoints.py` | Correct to `2025-03-01` |
| 6 | G-03b | `min_length` | "default 20 chars" | Code default = **3**; policy sets 20 | Correct to "code default 3; policy sets 20" |
| 7 | G-04b | `min_score` | "Default min_score=0.35" | Code default = **0.4**; policy sets 0.35 | Correct to "code default 0.4; policy sets 0.35"; also add intl-phone and date-ISO regex fallback patterns |
| 8 | G-05 | Pattern count | "~14 patterns" | Code has **17 patterns** (adds `slack-webhook`, `password-value`, `secret-keyword`, `generic-api-key`, `token-keyword`) | Update count to ~17 and add the five missing patterns |
