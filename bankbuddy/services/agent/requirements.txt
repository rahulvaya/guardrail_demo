fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.4.0
httpx>=0.27.0

# LangGraph + LangChain (Phase 1c)
langgraph>=0.2.50
langgraph-checkpoint-postgres>=2.0.0
langchain>=0.3.0
langchain-core>=0.3.0

# LLM gateway (provider-agnostic)
litellm>=1.50.0

# Azure AD / Entra ID auth for Azure OpenAI (used when LLM_API_KEY is empty)
azure-identity>=1.19.0

# Azure AI Foundry v2 project SDK (project-scoped connections, eval, agents).
# Inference still flows through LiteLLM via the unified Foundry endpoint;
# this SDK is for project metadata and future agent-service integration.
azure-ai-projects>=1.0.0

# DB driver for LangGraph PostgresSaver
psycopg[binary,pool]>=3.2.0

# Observability: ships traces, logs, and metrics to Application Insights
# when APPLICATIONINSIGHTS_CONNECTION_STRING is set.
# opentelemetry-instrumentation-openai wraps the openai SDK (used internally
# by LiteLLM) and emits gen_ai.* semantic-convention spans — required for the
# "Agents (preview)" blade in App Insights.
azure-monitor-opentelemetry>=1.6.0
opentelemetry-instrumentation-openai>=0.28b0
